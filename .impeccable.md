# CodeMax 设计上下文

## Design Context

### Users
桌面端 AI Agent 用户（Windows / Electron），在三类场景中工作：Work（办公文档/PPT/研究）、Coding（读写代码、终端、重构）、Design（生成与审查 UI）。他们是效率型专业用户，长时间停留在应用内，通过对话驱动 Agent 完成任务。

### Brand Personality
未来、精密、可信。三个词：**Liquid（流动）、Luminous（发光）、Calm（安静自信）**。界面应像一件来自近未来的苹果设备——材质真实、光感细腻，科技特效服务于状态表达而非炫耀。

### Aesthetic Direction
- 主方向：苹果 "iOS 27" 前瞻风格 = Liquid Glass（液态玻璃）演进版——半透明分层材质、backdrop blur + saturate、光谱边缘高光（inset 顶部高光）、连续大圆角（16–24px）、有色彩倾向的柔和投影。
- 反参考：不要赛博朋克霓虹紫蓝渐变、不要"AI 模板感"（深色底+荧光青）、不要卡片套卡片。
- 主题：深色/浅色双主题同等打磨。深色=深空墨蓝 + 极光环境光；浅色=磨砂白玻璃 + 柔和粉彩环境光。
- 强调色：沿用用户可自定义的 `--theme-color`，所有光效从它派生。

### Design Principles
1. **玻璃即层级**：层级通过材质（透明/磨砂/悬浮）表达，而不是通过边框和硬阴影。
2. **动效即反馈**：每个动画都必须回答"发生了什么"——悬停、按压、加载、生成中；装饰性动效只出现在欢迎屏与关键节点，且 500ms 内的交互反馈优先。
3. **性能是设计的一部分**：只动画 transform/opacity，backdrop-filter 仅限固定面板，全量支持 `prefers-reduced-motion`。
4. **一字排版的自信**：SF 系字体栈、紧凑字距的大标题、明确的模块化字级，正文 15px 保持可读。
5. **不缺一件旧物**：重设计不删除任何现有 UI 元素与功能入口，只做加法（如空状态引导、状态微光）。

### Constraints
- 生产级项目：禁止模拟数据，所有 UI 绑定真实 store 数据。
- 技术栈：Electron + React 19 + Tailwind CSS 3，动画走 GPU 合成层。
